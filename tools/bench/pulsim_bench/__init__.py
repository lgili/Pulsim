"""Pulsim developer benchmark CLI.

Single entry point (`pulsim-bench`) that unifies the historical
collection of standalone scripts in `benchmarks/`. Subcommands wrap
the existing runner modules so business logic is shared.

See `tools/bench/README.md` for install + usage.
"""

__version__ = "0.1.0"
